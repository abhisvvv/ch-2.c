#include <stdio.h>

int main(){

    int a=4;
    int b=6;
    int c=a+b;
    printf("the value of a %d\n and value of b %d\n and sum is %d\n",a,b,c);

    // mpdulus operator is used to get the remender

    printf("the remainder when a is dividded by is  :%d",a%b);

    // this does not work for exponentiation in c
    // int d= a^b;
    return 0;
}
